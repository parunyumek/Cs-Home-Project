"use server";

export async function register(fromData: FormData) {
  const fullname = fromData.get("fullname");
  const email = fromData.get("email");
  const fullname = fromData.get("fullname");
  const fullname = fromData.get("fullname");
}